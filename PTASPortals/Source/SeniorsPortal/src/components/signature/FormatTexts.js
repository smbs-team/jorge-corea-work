import { FormattedMessage } from 'react-intl';
import React from 'react';

export const continueLabel = (
  <FormattedMessage id="signature_continueLabel" defaultMessage="Continue" />
);

export const savingLabel = (
  <FormattedMessage id="signature_savingLabel" defaultMessage="Saving..." />
);

export const yourSignature = (
  <FormattedMessage
    id="signature_yourSignature"
    defaultMessage="Your signature"
  />
);

export const date = (
  <FormattedMessage id="signature_date" defaultMessage="Date" />
);

export const twoWitnessesConfirmation = (
  <FormattedMessage
    id="signature_twoWitnessesConfirmation"
    defaultMessage="I confirm that two witnesses or a county assessor observed me sign this application."
  />
);
