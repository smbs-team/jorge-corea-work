// formatText.tsx
/**
 * Copyright (c) King County. All rights reserved.
 * @packageDocumentation
 */

import { FormattedMessage } from 'react-intl';
import React from 'react';

export const findAsset = (
  <FormattedMessage
    id="business_find_asset"
    defaultMessage="Find the right asset category"
  />
);

export const searchByCategory = (
  <FormattedMessage
    id="business_search_by_category"
    defaultMessage="Search by category or item name"
  />
);

export const all = <FormattedMessage id="business_all" defaultMessage="All" />;

export const construction = (
  <FormattedMessage id="business_construction" defaultMessage="Construction" />
);

export const factory = (
  <FormattedMessage id="business_factory" defaultMessage="Factory" />
);

export const farm = (
  <FormattedMessage id="business_farm" defaultMessage="Farm" />
);

export const office = (
  <FormattedMessage id="business_office" defaultMessage="Office" />
);

export const retail = (
  <FormattedMessage id="business_retail" defaultMessage="Retail" />
);

export const service = (
  <FormattedMessage id="business_service" defaultMessage="Service" />
);
