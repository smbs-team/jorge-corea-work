import React from "react";
import { SingleLabel as Single } from "@ptas/react-ui-library";

const SingleLabel = () => {
  return <Single text="hola" textValue="123" defaultText="Texto por defecto"/>;
};

export default SingleLabel;
